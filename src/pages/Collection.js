import Saved from "../components/Saved";

function Collection() {
    return (
        <div style={{marginTop: "7%", textAlign: "center"}} className="display">
            <h1>Collection</h1>
            <Saved />
        </div>
    )
}

export default Collection;